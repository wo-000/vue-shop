import { request } from "./config";

export function shopDetail(url, params) {
  return request({
    url,
    params
  });
}
export function recDetail(url, params) {
  return request({
    url,
    params
  });
}
