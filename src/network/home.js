import { request } from "./config";

export function homeAjax(url, params) {
  return request({
    url,
    params
  });
}

export function starsData(url, params) {
  return request({
    url,
    params
  });
}

export function rankList(url, params) {
  return request({
    url,
    params
  });
}

export function recommendAjax(url, params) {
  return request({
    url,
    params
  });
}
