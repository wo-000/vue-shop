import { request } from "./config";

export function shopListsAjax(url, params) {
  return request({
    url,
    params
  });
}

export function shopAjax(url, params) {
  return request({
    url,
    params
  });
}
